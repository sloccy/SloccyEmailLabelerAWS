{
  "private": true,
  "description": "Pinned front-end vendor deps. Dependabot bumps these; scripts/vendor.sh materializes them into static/vendor/ at build time (see Makefile). npm itself is never run.",
  "dependencies": {
    "bootstrap": "5.3.8",
    "htmx.org": "2.0.10"
  }
}
